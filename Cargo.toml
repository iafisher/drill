[package]
name = "popquiz"
version = "0.1.0"
authors = ["Ian Fisher <iafisher@protonmail.com>"]
edition = "2018"

[dependencies]
argparse = "0.2.2"
chrono = { version = "0.4", features = ["serde"] }
rand = "0.6"
serde = { version = "1.0.92", features = ["derive"] }
serde_json = "1.0.39"
